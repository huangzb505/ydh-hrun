-   test:
        name: 管理端_订单作废
        dependent:
        -   管理端_订单退回
        request:
            data: {}
            headers:
                Content-Type: application/x-www-form-urlencoded;charset=UTF-8
            method: POST
            params:
                access_token: $global_company_access_token
                orderNum: $global_order_create_orderType_1_orderNum
                remark: test
                version: '3'
            url: https://api.dinghuo123.com/order/order_cannel.json
        validate:
        - "eq": [content.code,200]
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - text/html;charset=UTF-8
        - "eq": [content.data.dispalyStatus,已作废]
        - {"check": "content.data", "comparator": "count_gt", "expect": 0}