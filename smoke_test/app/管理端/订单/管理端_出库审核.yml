-   test:
        name: 管理端_出库审核
        dependent:
        -   管理端_财务审核
        -   管理端_仓库列表
        request:
            data:
                access_token: $global_company_access_token
                billEntries: ${turn_json($billEntries)}
                orderNum: $global_order_create_orderType_1_orderNum
                warehouseId: $warehouse_default_id
            headers:
                Content-Type: application/x-www-form-urlencoded;charset=UTF-8
            method: POST
            url: https://api.dinghuo123.com/logistics/outStorage.json
        variables:
            billEntries:
            -   mainCount: $order_audit_count
                mainUnitId: $order_audit_mainUnitId
                productCode: $order_audit_productCode
                productId:
        extract:
        -   outStorage_billNum: content.data.billNum
        validate:
        - "eq": [content.code,200]
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - text/html;charset=UTF-8
        - {"check": "content.data", "comparator": "count_gt", "expect": 0}