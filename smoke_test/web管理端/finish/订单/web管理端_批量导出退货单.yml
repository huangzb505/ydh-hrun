-   test:
        dependent:
        - web管理端_管理账号登录_login
        - web管理端_系统配置
        - web管理端_退单列表
        name: web管理端_批量导出退货单
        request:
            method: GET
            params:
                file_token: $global_runtime_config_file_token
                orderNums: ${turn_json($order_list_type_2_orderNum)}
            url: https://file.dinghuo123.com/corp/order/batchReturnOrderExport
        validate:
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - application/vnd.openxmlformats-officedocument.spreadsheetml.sheet;charset=UTF-8
