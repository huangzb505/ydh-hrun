-   test:
        dependent: web管理端_管理账号登录_login
        name: web管理端_新增客户级别
        request:
            data:
                discountrate: '0.6'
                name: ${short_uuid()}
            headers:
                Content-Type: application/x-www-form-urlencoded;charset=utf-8
                X-Requested-With: XMLHttpRequest
            method: POST
            url: https://corp.dinghuo123.com/v2/customerType/add
        extract:
        - global_customerType_add_id: content.data.id
        validate:
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - application/json;charset=utf-8
        -   eq:
            - content.code
            - 200
        -   eq:
            - content.message
            - 操作成功
        -   check: content.code
            comparator: check_type_and_exist
            expect: int
        -   check: content.message
            comparator: check_type_and_exist
            expect: str
        -   check: content.data.name
            comparator: check_type_and_exist
            expect: str
        -   check: content.data.isDefault
            comparator: check_type_and_exist
            expect: str
        -   check: content.data.id
            comparator: check_type_and_exist
            expect: int
        -   check: content.data.discountrate
            comparator: check_type_and_exist
            expect: float
        -   check: content.data
            comparator: check_type_and_exist
            expect: dict
        -   check: content
            comparator: check_type_and_exist
            expect: dict
