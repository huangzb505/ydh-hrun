- config:
        output:
        -  customer_add_id
-   test:
        dependent:
        - web管理端_管理账号登录_login
        - web管理端_新增客户
        - web管理端_新增客户
        name: web管理端_批量开通帐号
        request:
            data:
                accountPrefix: xuekj
                customerIds: ${turn_json($customerIds)}
                password: '123456'
            headers:
                Content-Type: application/x-www-form-urlencoded;charset=utf-8
                X-Requested-With: XMLHttpRequest
            method: POST
            url: https://corp.dinghuo123.com/v2/customer/batchActivate
        variables:
        -    customerIds:
              -  ${get_list_item($customer_add_id,0)}
              -  ${get_list_item($customer_add_id,1)}
        validate:
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - application/json;charset=utf-8
        -   eq:
            - content.code
            - 200
        -   eq:
            - content.message
            - 批量启用成功
        -   eq:
            - content.data
            - ''
        -   check: content.code
            comparator: check_type_and_exist
            expect: int
        -   check: content.message
            comparator: check_type_and_exist
            expect: str
        -   check: content.data
            comparator: check_type_and_exist
            expect: str
        -   check: content
            comparator: check_type_and_exist
            expect: dict
