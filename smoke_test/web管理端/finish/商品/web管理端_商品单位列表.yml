-   test:
        dependent: web管理端_管理账号登录_login
        name: web管理端_商品单位列表
        request:
            headers:
                X-Requested-With: XMLHttpRequest
            method: GET
            params:
                t: ${get_time()}
            url: https://corp.dinghuo123.com/v2/goodsUnit/list
        extract:
        -   goodsUnit_list_id: $.data[*].id
        -   goodsUnit_list_name: $.data[*].name
        idempotency: True
        validate:
        -   eq:
            - status_code
            - 200
        -   eq:
            - headers.Content-Type
            - application/json;charset=utf-8
        -   eq:
            - content.message
            - 操作成功
        -   eq:
            - content.code
            - 200
        -   check: content.data.0.id
            comparator: check_type_and_exist
            expect: int
        -   check: content.data.0.status
            comparator: check_type_and_exist
            expect: str
        -   check: content.data.0.name
            comparator: check_type_and_exist
            expect: str
        -   check: content.data.0
            comparator: check_type_and_exist
            expect: dict
        -   check: content.data
            comparator: check_type_and_exist
            expect: list
        -   check: content.message
            comparator: check_type_and_exist
            expect: str
        -   check: content.code
            comparator: check_type_and_exist
            expect: int
        -   check: content
            comparator: check_type_and_exist
            expect: dict
